lb:
  extends:
    file: common.yml
    service: lb
  links:
    - dockerapp

dockerapp:
  extends:
    file: common.yml
    service: dockerapp
  image: jleetutorial/dockerapp 
  links:
    - redis

redis:
  extends:
    file: common.yml
    service: redis
